extends _layout.pug
include mixins.pug

block styles
    link(rel="stylesheet" href="/static/css/portal.css")

block main-content
    .row
        .col-sm-11
            h2 Latest news
        .col-sm-1
            а.btn.btn-default See More
    .row.row.extra-bottom-padding
        +list-articles(params)
        +list-articles(params)
        +list-articles(params)
    .row
        .col-sm-11
            h2 Latest reviews
        .col-sm-1
            а.btn.btn-default See More
    .row.row.extra-bottom-padding
        +delete-that-mixin-later
        +delete-that-mixin-later
        +delete-that-mixin-later
    .row
        .col-sm-11
            h2 Latest guides
        .col-sm-1
            а.btn.btn-default See More
    .row.row.extra-bottom-padding
        +delete-this-too
        +delete-this-too
        +delete-this-too
